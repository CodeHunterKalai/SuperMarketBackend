# Server
server.port=8080
spring.application.name=supermarket-management

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/supermarket_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Kalai@2111
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Disable H2
spring.h2.console.enabled=false 

# CORS
cors.allowed.origins=http://localhost:3000,http://localhost:5173
